@model IEnumerable<Capa_Datos.USUARIO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CONTRASENA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ACTIVO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERSONA.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_USUARIO.USUARIO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CONTRASENA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ACTIVO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERSONA.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_USUARIO.USUARIO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NOMBRE_USUARIO }) |
            @Html.ActionLink("Details", "Details", new { id=item.NOMBRE_USUARIO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NOMBRE_USUARIO })
        </td>
    </tr>
}

</table>
